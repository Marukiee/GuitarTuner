<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real styling; this only kills the platform ActionBar and gives us
         a transparent system-bar surface for enableEdgeToEdge(). -->
    <style name="Theme.GuitarTuner" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
